# Railway — Account_Core UI (comptabilite-ui)
# Service settings → Root Directory MUST be: comptabilite-ui
# (Repo github.com/gbpay365/Account_Core — root is NOT Account/)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
